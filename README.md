# YouTube to MP3 Converter

This repository contains a script to convert YouTube videos to MP3 audio format.

## Installation

You need to install the required dependencies listed in `requirements.txt`.

## Usage

Run the `converter.py` script with the desired YouTube video URL.
